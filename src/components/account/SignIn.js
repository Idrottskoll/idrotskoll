import React from 'react';
import { Input, Button } from '../styles';

const SignIn = () => (
    <React.Fragment>
        <Input />
        <Button>Logga in</Button>
    </React.Fragment>
);

export default SignIn;
