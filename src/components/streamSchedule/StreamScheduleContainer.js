import React, { Component } from 'react';
import { Button, Label, Input, TextArea, Title, Paragraph, Link, SmallTitle } from '../styles';

export default class StreamScheduleContainer extends Component {
    render() {
        return (
            <div>
                <Paragraph>This is the constainer</Paragraph>
            </div>
        );
    }
}
